<%= form_for @recipe do |r| %>
<%= r.label :title %>
  <%= r.text_field :title %><br>

  <%= r.fields_for :ingredients do |ing| %>
    <%= ing.label :name %>
    name: <%= ing.text_field :name %><br>

    <%= ing.label :quantity %>
    qty: <%= ing.integer :quantity %><br>

  <% end %>
  <%= link_to 'add ingredient', @recipe %>
  </ul>
  <%= r.submit %>
<% end %>
